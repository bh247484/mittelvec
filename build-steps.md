# Generate Single-Header Library
Generate the header: `python3 generate_single_header.py`

# Compile Test App
1. `clang++ -std=c++17 TestSingleHeader.cpp -o TestSingleHeader`
2. Run: `./TestSingleHeader`